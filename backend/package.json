{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend",
  "main": "index.js",
  "dependencies": {
    "koa": "^2.6.2"
  },
  "devDependencies": {
    "babel-register": "^6.26.0"
  },
  "scripts": {
    "start": "node index.js",
    "clean": "rm -rf dist",
    "build": "npm run clean && babel src -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
